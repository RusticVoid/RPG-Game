function love.conf(t)
    t.window.title = "Small RPG"         -- The window title (string)
    t.window.resizable = false          -- Let the window be user-resizable (boolean)
    t.window.vsync = true          -- Let the window be user-resizable (boolean)
end